{
  "name": "sql-canister-rust",
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "prebuild": "npm run prebuild --workspaces --if-present",
    "dev": "npm run dev --workspaces --if-present",
    "test:canister": "node tests/backend.canister.test.js",
    "test:file-import": "node tests/file-import.test.js"
  },
  "type": "module",
  "devDependencies": {
    "@dfinity/agent": "2.2.0",
    "@dfinity/candid": "2.2.0"
  },
  "workspaces": [
    "frontend"
  ]
}
